apiVersion: v1
kind: Service
metadata:
  name: cloudmart-backend-service
spec:
  selector:
    app: cloudmart-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP